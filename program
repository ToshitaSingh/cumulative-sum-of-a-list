# Python code to get the Cumulative sum of a list 
lists = [10, 20, 30, 40, 50]  
cu_list = [] 
a=0
for i in range(0,len(lists)):
    a+=lists[a]
    cu_list.append(a)
  
print(Cu_list)

# browser based vs code environment with github (.)
# press period (.) key on any repository
